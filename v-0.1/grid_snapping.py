# def snap_to_grid(point, grid_size):
#     # Snap a point to the nearest grid coordinates
#     snapped_x = round(point[0] / grid_size) * grid_size
#     snapped_y = round(point[1] / grid_size) * grid_size
#     return (snapped_x, snapped_y)
