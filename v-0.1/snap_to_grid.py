# def draw_with_grid(canvas, start_point, end_point, color, brush_size, grid_size=20):
#     start_point = (start_point[0] // grid_size * grid_size, start_point[1] // grid_size * grid_size)
#     end_point = (end_point[0] // grid_size * grid_size, end_point[1] // grid_size * grid_size)
#     cv2.line(canvas, start_point, end_point, color, brush_size)
#     return canvas
